# Voxprint (formerly Echo Transcription) Roadmap

## Shipped
- [x] iOS 1.3.6 (Voxprint rename) — built, uploaded, submitted 2026-08-03. Build `202608031830` (id `250e6d44-dc13-4adc-948d-ba1351910d5f`), VALID, attached to version `e6cd1dcd-49ec-4633-b9d3-00cfe68513e2`, review submission `31fe0d40-eae5-4887-86b9-5f4234e48149` → **WAITING_FOR_REVIEW**. iOS 1.3.5 is now READY_FOR_SALE.
- [x] App Store description rewritten Echo → Voxprint. The v1.3.6 en-US description still named the app "Echo" throughout (including an "ECHO PRO" section) — the exact name App Review rejected. Rewritten to Voxprint plus a rename-focused What's New.

## Follow-ups (actionable, not blocked)
- [x] macOS rebranded and SHIPPED 2026-08-03. macOS **1.3.6** (not 1.3.4 — `project.yml` shares one `MARKETING_VERSION`, already 1.3.6, so forcing 1.3.4 would need a per-target override; 1.3.6 > 1.3.3 so it's a valid bump and unifies both platforms). Build `202608032100` (id `9c8743b1-11bd-41e2-8a50-e32d5055db9d`) VALID, version `fb8c5c1e-dbb6-4776-883d-e03ffb4f0bfe`, submission `e671bcb8-8170-42fe-a3f9-856ce1bd4ffa` → **WAITING_FOR_REVIEW**. Mac store description also still said "ECHO PRO"/"WHY ECHO" — replaced with the Voxprint copy, Mac-adapted ("on your Mac"). Stale Echo-branded screenshot deleted, fresh 1280x800 capture uploaded.
- NOTE (not a task): `asc submission-health` is NOT a real subcommand (it's a skill name). Real paths: `asc review status`, `asc review doctor`, `asc review submissions-list`.
- NOTE (not a task): `asc review submit` is unreliable on this account — it failed with "review submission … does not contain target version" even though the submission *did* contain the version. Working sequence: `asc versions attach-build --version-id … --build-id …` then `asc review submissions-submit --id … --confirm`.

## Blocked on Joshua
- [x] ~~**iOS 1.3.6 is in review with "Echo" still in the in-app UI.**~~ **RESOLVED 2026-08-03** — Joshua authorized forfeiting queue position. Cancelled submission `31fe0d40`, rebuilt from current code as build `202608032330`, and verified on a simulator that the header/splash actually render "Voxprint" before archiving (source diff alone was what got missed last time). Resubmitted: version `e6cd1dcd` IOS 1.3.6 WAITING_FOR_REVIEW via submission `a084a3e5`. Commit `eca7e86`.
- [x] ~~Stray empty review submission `c59e62cd`~~ — superseded 2026-08-09, see the resolved ASC-issue entry below (now 3 uncancellable empty drafts, not 1; still believed harmless).
- [x] **Marketing domain — DONE 2026-08-06.** `marketingUrl` updated to `https://voxprint.heyitsmejosh.com` on the WAITING_FOR_REVIEW version (e6cd1dcd, v1.3.6). READY_FOR_SALE version (fb8c5c1e) rejected the edit ("cannot be edited at this time" — locked live version); the in-review version carries it forward once approved.
- [ ] **Icon direction decision.** Current blue mic mark (clrs.cc #0074D9) reads generic. Needs Joshua's call on the new direction before any redesign — this is a taste/design judgement, not something to guess at. No command; just a decision, then the icon regen + a version bump to ship it.
- [ ] **Delete the duplicate Mac ASC record `6783015101` ("Echo Transcribe Mac" / now listed as "Transcriptly", macOS 9.9.9 Developer Rejected).** Dashboard-only — the API delete is refused while the version sits in a rejected state. Apple support case **102949489427** (an earlier note cites 102949488998 — confirm which is live). Joshua said 2026-07-29 he'd delete it from the ASC app-info screen himself. Check for an Apple reply, then retry `asc web apps delete --app 6783015101`, or delete via appstoreconnect.apple.com.

## From Apple Notes (imported 2026-08-08)
- [x] **ASC issue RESOLVED 2026-08-09.** Re-investigated the `a084a3e5` submission: the "phantom IAP" read was wrong — voxprint's one IAP (`Echo Pro` / `com.nulljosh.echo.unlock`, id `6787371864`) is APPROVED and legitimately used, `asc validate iap` returned 0 issues. The actual problem was a stuck submission in state `UNRESOLVED_ISSUES` blocking new submits, plus `asc review submit` retries spawning several empty duplicate `READY_FOR_REVIEW` drafts (ids `c59e62cd`, `0e8f0db7`, `ce2d5f3c`) that `submit cancel` refuses to cancel ("not in cancellable state") — likely harmless, same class as a prior Curvely leftover, clear from the dashboard if they ever cause a conflict again. Fix: cancelled `a084a3e5`, then used the low-level API directly (`asc review items-add` + `asc review submissions-submit --id <id> --confirm`) instead of the flaky high-level `asc review submit`. Version `e6cd1dcd` (1.3.6, build `925a0039`) is now **WAITING_FOR_REVIEW** via submission `434fce87`. Build validated clean (0 blocking errors — only 2 info-level notes: manual release type, App Privacy publish unverifiable via API). macOS 1.3.6 remains READY_FOR_DISTRIBUTION/live, unaffected. GitHub repo rename Echo→Voxprint already done, nothing to do there.
- [ ] Website *content* still says Echo after the rename (the domain half is resolved above) — rewrite `web/index.html` copy Echo → Voxprint.
